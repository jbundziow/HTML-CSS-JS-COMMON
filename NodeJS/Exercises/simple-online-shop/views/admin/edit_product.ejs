<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Shop - edit product</title>
    <link rel="stylesheet" href="/css/common.css" />
    <link rel="stylesheet" href="/css/admin/add_new_product.css" />
  </head>
  <body>
    <nav>
      <ul>
        <li><a href="/">Home</a></li>
        <li>
          <a href="/cart">Cart [<span id="productsSumInCart">0</span>]</a>
        </li>
        <li><a href="/admin" class="active">Admin</a></li>
      </ul>
    </nav>

    <div class="container">
      <h2>Edit existing product</h2>
      <form id="add-product-form" method="post" action="./<%=productData.id%>">
        <div class="form-group">
          <% if(error) {%>
          <h3 class="form-bad-data">You passed wrong data! Try again.</h3>
          <% } %>
          <p class="prodID">Product ID: <span><%=productData.id%></span></p>
        </div>
        <div class="form-group">
          <label for="title">Title:</label>
          <input
            type="text"
            id="title"
            name="title"
            required
            value="<%=productData.title%>"
          />
        </div>
        <div class="form-group">
          <label for="description">Description:</label>
          <textarea id="description" name="description" rows="4" required>
<%=productData.description%></textarea
          >
        </div>
        <div class="form-group">
          <label for="price">Price:</label>
          <input
            type="number"
            id="price"
            name="price"
            min="0"
            step="0.01"
            required
            value="<%=productData.price.toFixed(2)%>"
          />
        </div>
        <button type="submit" class="acknowledge-button">Save changes</button>
      </form>
    </div>
    <script type="text/javascript" src="/scripts/localStorage.js"></script>
  </body>
</html>
