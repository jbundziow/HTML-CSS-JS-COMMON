<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Sklep - strona główna</title>
    <link rel="stylesheet" href="/css/common.css" />
    <link rel="stylesheet" href="/css/admin/add_new_product.css" />
  </head>
  <body>
    <nav>
      <ul>
        <li><a href="/">Home</a></li>
        <li><a href="/cart">Cart [0]</a></li>
        <li><a href="/admin" class="active">Admin</a></li>
      </ul>
    </nav>

    <div class="container">
      <h2>Add New Product</h2>
      <form id="add-product-form" method="post" action="./add_new_product">
        <div class="form-group">
          <% if(error) {%>
          <h3 class="form-bad-data">You passed wrong data! Try again.</h3>
          <% } %>
          <label for="title">Title:</label>
          <input type="text" id="title" name="title" required />
        </div>
        <div class="form-group">
          <label for="description">Description:</label>
          <textarea
            id="description"
            name="description"
            rows="4"
            required
          ></textarea>
        </div>
        <div class="form-group">
          <label for="price">Price:</label>
          <input
            type="number"
            id="price"
            name="price"
            min="0"
            step="0.01"
            required
          />
        </div>
        <button type="submit" class="acknowledge-button">
          Add product to the shop
        </button>
      </form>
    </div>
  </body>
</html>
